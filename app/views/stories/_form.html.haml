.container
  %h1 Submit a Story
  = form_for(@story) do |f|
    = hidden_field_tag :owner_id, current_user.id

    = f.label :title
    = f.text_field :title, class: 'form-control'
    = f.label :attachment, "Story Photo"
    = f.file_field :attachment, class: "form-control"
    = f.label :youtube_link
    = f.text_area :youtube_link, class: 'form-control wysihtml5'    
    = f.label :department_id
    = f.text_field :department_id, class: 'form-control'
    = f.label :sponsor_id
    = f.text_field :department_id, class: 'form-control'
    %p
    = f.submit "Submit Post", class: "btn btn-primary"