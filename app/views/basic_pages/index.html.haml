%h1 Listing basic_pages

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @basic_page.each do |basic_page|
      %tr
        %td= link_to 'Show', basic_page
        %td= link_to 'Edit', edit_basic_page_path(basic_page)
        %td= link_to 'Destroy', basic_page, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Health resource', new_basic_page_path
